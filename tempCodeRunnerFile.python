import socket

target = input("IP kiriting: ")

for port in range(1, 100):
    s = socket.socket()
    s.settimeout(0.5)
    
    try:
        s.connect((target, port))
        print(f"Port ochiq: {port}")
    except:
        pass